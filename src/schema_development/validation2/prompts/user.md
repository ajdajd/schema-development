# Frozen Data Snapshot Metadata Schema v1.1.1

{{SCHEMA_MARKDOWN}}

# Snapshot

- File: `{{SNAPSHOT_FILE_NAME}}`
- Source corpus: `{{SOURCE}}`
- Artifact type: `{{ARTIFACT_TYPE}}`

# Parent-document context

The following metadata may be used to understand the snapshot, but the parent document is not the object being validated.

{{DOCUMENT_METADATA}}

Evaluate the attached snapshot using the system instructions. Return the structured coverage assessment only.
